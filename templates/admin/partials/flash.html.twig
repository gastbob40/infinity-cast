{% for type, messages in app.session.flashbag.all() %}
    {% for message in messages %}
        <alert-floating type="{{ type }}"
                        {% if duration is defined and type == 'success' %}duration="{{ duration }}"{% endif %}>
            {{ message | nl2br }}
        </alert-floating>
    {% endfor %}
{% endfor %}